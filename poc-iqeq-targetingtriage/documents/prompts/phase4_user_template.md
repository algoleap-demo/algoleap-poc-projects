Here is the scored accounts payload (JSON array). Use it as the only source of truth.

Return a JSON array with items shaped exactly like:
[
  {
    "account_id": "ACC_LU_0001",
    "priority_bucket": "A",
    "rationale_text": "1-3 sentences explaining the priority using the provided fields."
  }
]

Scored accounts:
{{SCORED_ACCOUNTS_JSON}}
